package Office_Hours.Practice_07_13_2020;

public class FrequancyOFCharacters {


    public static void main(String[] args) {
/*
        String str = "OOPQQ";
        String nonDup = "";

        for (int i = 0; i < str.length(); i++) {
            String s = "" + str.charAt(i);
            if (!nonDup.contains(s)) {
                nonDup += s;
            }
        }
        for(int j=0;j<= str.length()-1;j++) {
        char c1 = nonDup.charAt(j);
        int count1 = 0;

            char  each = str.charAt(j);
            if(each == c1);
            count1 +=1;
        }
        String result = "";

        System.out.println(result);

  /*      char ch1 = nonDup.charAt(0);
        int count1 = 0;
        for (int j = 0; j <= str.length() - 1; j++) {
            char each = str.charAt(j);
            if (each == ch1) {
                count1 += 1;
                result += "" + ch1 + count1;

                char ch2 = nonDup.charAt(1);
                int count2 = 0;
                for (int i = 0; i <= str.length() - 1; i++) {
                    char each1 = str.charAt(i);
                    if (each1 == ch2) {
                        count2 += 1;
                        result += "" + ch1 + count2;

                        char ch3 = nonDup.charAt(1);
                        int count3 = 0;
                        for (int t = 0; t <= str.length() - 1; t++) {
                            char each3 = str.charAt(i);
                            if (each3 == ch3) {
                                count3 += 1;
                                result += "" + ch3 + count3;
                            }
                        }
                    }
*/


    }

}